# Supabase values are used by server-side tooling and must not expose service keys in the Android client.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Configure FCM server credentials only in deployment secrets, never in this file.
FCM_PROJECT_ID=soundguard-dd7b4
